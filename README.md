# Knowledge Graph
A simple knowledge graph implementation in Python.

## Usage
1. Create a `KnowledgeGraph` instance.
2. Add `Runbook` instances to the graph using the `add_runbook` method.
3. Search for runbooks using the `search` method.

## Testing
Run `pytest` to execute the tests.
