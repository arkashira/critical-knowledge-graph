# Audit Trail

This project generates a PDF audit trail of all runbook changes and uses.

## Usage

1. Create a list of audit trail entries with user, action, and metadata.
2. Call the `generate_audit_trail` function to generate the PDF content.
3. The PDF content will be digitally signed and tamper-evident.

## Testing

Run the tests using `pytest` to ensure the implementation is correct.
